CRYPTO_REPO_PATH=../crypto
CRYPTO_SWING_CONFIG=config/swing.yaml
CRYPTO_SWING_MODE=shadow

# Existing repository credentials remain owned by the existing repository.
# Do not duplicate secrets here unless the existing adapter explicitly requires them.
BITVAVO_API_KEY=
BITVAVO_API_SECRET=
CMC_PRO_API_KEY=
COINMARKETCAP_API_KEY=

# Live stays unavailable unless explicitly acknowledged.
CRYPTO_SWING_LIVE_ACK=
