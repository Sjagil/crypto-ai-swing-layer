# Reference repositories

Run:

```bash
python scripts/clone_references.py
```

to clone only references marked `enabled: true` in `config/references.yaml`.

Third-party code is intentionally not vendored into this ZIP.
